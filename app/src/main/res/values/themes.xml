<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PetAllies" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/Blue</item>
        <item name="android:statusBarColor">@color/Blue</item>
    </style>

    <style name="Theme.PetAllies" parent="Base.Theme.PetAllies" />

    <style name="CustomProgressBar" parent="android:Widget.ProgressBar.Large">
        <item name="android:progressDrawable">@drawable/custom_progress_bar</item>
    </style>
</resources>